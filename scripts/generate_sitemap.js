const fs=require('fs');const path=require('path');const ROOT=process.cwd();const SITE='https://tosborisoglebsk.ru';const urls=new Set();const today=new Date().toISOString().slice(0,10);const skip=new Set(['.git','.github','node_modules','scripts','admin','audit','_private','tools']);function walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){if(entry.isDirectory()&&skip.has(entry.name))continue;const full=path.join(dir,entry.name);if(entry.isDirectory())walk(full);else if(entry.name==='index.html'){const rel=path.relative(ROOT,path.dirname(full)).split(path.sep).join('/');if(rel.startsWith('documents/demo'))continue;urls.add(rel?`${SITE}/${rel}/`:`${SITE}/`)}}}walk(ROOT);const rows=[...urls].sort().map(loc=>`  <url><loc>${loc}</loc><lastmod>${today}</lastmod></url>`).join('\n');fs.writeFileSync(path.join(ROOT,'sitemap.xml'),`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${rows}\n</urlset>\n`,'utf8');console.log(`Generated sitemap URLs: ${urls.size}`);