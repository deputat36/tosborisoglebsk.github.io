# Run notes 2026-07-01

This note records manual checks that are not yet part of the main audit runner.

Standalone scripts currently added:

- `scripts/audit_readiness_gate.js`
- `scripts/audit_verification_control_content.js`
- `scripts/audit_verification_tasks_content.js`

Recent confirmed checks through GitHub fetch:

- `data/verification_readiness_matrix.csv` exists and contains the four priority slugs.
- `verification-tasks/index.html` exists and is marked `noindex,nofollow`.
- `verification-control/index.html` exists and is marked `noindex,nofollow`.
- `calendar/index.html` and `calendar/action-routes/index.html` exist and contain public event routes.

Actions status:

- Workflow runs were not returned for commits `4c6462fbbc4ad9ec5c0419f10e5fd8cfeaf1ee49` and `cc30e9b0f7c48f8b651256d12a78d9c0d790354e` through the commit workflow endpoint.
- GitHub Actions UI still needs manual review before treating recent standalone audits as CI-checked.
