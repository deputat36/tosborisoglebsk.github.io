# Продолжение аудита портала ТОС БГО от 27 июня 2026 года, часть 10

Этот журнал продолжает `docs/AUDIT-CONTINUATION-2026-06-27-PART-9.md` после пункта 112.

## 113. Усиление машинной проверки страницы открытых данных

- усилен существующий аудит `scripts/audit_open_data_links.js`, который уже подключён в общий project runner;
- помимо проверки ссылок на JSON, CSV, RSS и sitemap теперь проверяются язык страницы, title, canonical или Open Graph URL, тип Open Graph website, основной контент и подключение общего скрипта сайта;
- закреплено назначение страницы: данные портала хранятся в простых JSON и CSV-файлах и используются для проверки каталога, отчётов, поиска пробелов и планирования обновлений;
- проверяются ключевые карточки открытых данных: каталог ТОС, новости, проекты, потребности, сделано, качество данных и источники данных;
- сохранена проверка обязательных файлов: `toses.json`, `news.json`, `projects.json`, `needs.json`, `done.json`, `site_health.json`, `page_index.json`, `rss.xml` и `sitemap.xml`;
- проверяются предупреждения: рабочий статус не означает подтверждение факта, для публикации нужны источник, дата актуальности и разрешения, устаревшие или спорные сведения проходят повторную проверку.

## Практический эффект

Страница открытых данных теперь защищена не только от битых ссылок на файлы, но и от потери смысловых предупреждений. Если исчезнут правила использования CSV/JSON, ключевые карточки данных или caveat о неподтверждённых рабочих статусах, общий аудит это покажет.

## Ограничения

- `open-data/index.html` не изменялся;
- JSON, CSV, RSS, sitemap, карточки ТОС, статусы и публичные данные не менялись;
- новый отдельный audit-файл не создавался, потому что был усилен уже подключённый `scripts/audit_open_data_links.js`;
- CI/Actions логи не проверялись.
