# Audit continuation part 15

## 118. Needs audit update

- updated `scripts/audit_needs_integrity.js`;
- added check for `/tos/{slug}/` when `tos_slug` is set;
- `data/needs.json` was not changed;
- public pages were not changed;
- CI logs were not checked.
