# Продолжение аудита портала ТОС БГО от 27 июня 2026 года, часть 14

Этот журнал продолжает часть 13 после пункта 116.

## 117. Отдельная проверка обязательных проектных ID

- добавлен `scripts/audit_projects_required_ids.js`;
- проверяется наличие `data/projects.json`;
- проверяется, что файл проектов является массивом;
- закреплены обязательные ID: `child-sport-playground`, `memorial-renovation`, `water-supply-improvement`;
- для каждого ID проверяется наличие публичной страницы `/projects/{id}/`;
- основной `scripts/audit_projects_integrity.js` не изменен, потому что его полная запись была заблокирована инструментом.

## Практический эффект

Если из банка проектов исчезнут базовые форматы детской площадки, памятного места или водоснабжения, отдельный запуск аудита это покажет.

## Ограничения

- `data/projects.json` не изменялся;
- публичные страницы проектов не изменялись;
- общий runner пока не содержит новый скрипт;
- скрипт запускается отдельно командой `node scripts/audit_projects_required_ids.js`;
- CI/Actions логи не проверялись.
