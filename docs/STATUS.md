# Состояние проекта

Обновлено: 3 сентября 2026 года.

## Краткий вывод

Портал ТОС БГО имеет устойчивый технический каркас, единый генерационный pipeline и блокирующие проверки качества. Работают каталог ТОС, отдельные карточки территорий, новости, проекты, потребности, события, истории результата, редакционные инструменты, проектные формы, открытые данные и управленческая сводка `/site-health/`.

Основной оставшийся риск — не устройство сайта, а достоверность сведений, доля подтверждённого контента и получение разрешений на публикацию контактов и материалов.

Документ описывает только фактическое состояние основной ветки `release-2025-12-22`. Закрытый без слияния PR #220 не считается источником состояния; учитываются только независимо перенесённые и повторно проверенные изменения.

## Актуальные метрики основной ветки

По `data/site_health.json`:

- управляемых HTML-страниц: 368;
- публичных страниц: 309;
- служебных страниц `noindex`: 59;
- битых внутренних ссылок и якорей: 0;
- базовых SEO-предупреждений: 0;
- карточек ТОС: 24;
- карточек высокого приоритета: 4;
- карточек со статусом `verified`: 0;
- карточек со статусом `partial`: 8;
- карточек `needs_review`: 16;
- средняя заполненность карточек: 74%;
- общая оценка `site_health`: 16/100.

Технический accessibility/performance-аудит имеет более широкую область обхода и проверяет 370 HTML-файла. В нём:

- публичных страниц: 309;
- страниц с замечаниями: 0;
- high / medium / low: 0 / 0 / 0;
- публичных runtime-ресурсов: 113;
- ресурсов сверх бюджета: 0;
- общий CSS: 46 075 байта;
- общий JavaScript: 665 183 байт.

Разница между 368 и 370 страницами связана с разными областями обхода отчётов и не является ошибкой подсчёта. В бюджеты ресурсов входят только браузерные файлы из публичного runtime-контура; служебные скрипты и документационные visual baseline PNG исключены.

Полный базовый аудит основной ветки: `docs/AUDIT-2026-07-10.md`.
Текущий журнал автономной работы: GitHub issue #9.

## Завершённые технические циклы в основной ветке

### Стабилизация CI и генерации

- убран лишний запуск каждые 30 минут;
- включён `cancel-in-progress`;
- устранена рекурсия audit-runner;
- добавлены тайм-ауты, компактная диагностика и Actions-артефакт;
- sitemap использует реальные даты изменения файлов;
- генерация новостей, проектов и материалов объединена в один автоматический pipeline;
- узкие генераторы оставлены только для ручного запуска;
- scheduled VK-news включён в общий контур миграции, генерации, sitemap, ссылок и SEO;
- локальный `audit:all` пересобирает `/actions-check/` после обновления `site_health.json`, поэтому project-mode не получает рассинхронизированные generated-at markers;
- временные диагностические файлы удаляются до автокоммита.

Основные PR: #174 и #176.

### Происхождение и доверие к контенту

Введено поле `content_origin`:

- `verified` — материал имеет явный проверяемый источник;
- `editorial` — редакционный материал, который не считается автоматически подтверждённым;
- `starter` — стартовая идея, а не утверждённый проект;
- `request` — запрос сведений или заготовка, а не подтверждение события или результата.

Текущая картина `data/content_origin_report.json`:

- всего материалов: 169;
- `verified`: 7;
- `editorial`: 81;
- `starter`: 24;
- `request`: 57;
- подтверждённый контент есть у 7 из 24 ТОСов;
- у 13 из 24 ТОСов есть только стартовые идеи или запросы;
- карточек без какого-либо контента: 0.

Основной PR: #175.

### Доступность и производительность

Автоматически проверяются:

- `lang` у HTML;
- основной landmark `<main>`;
- skip-link;
- `alt` у изображений;
- `type` у кнопок;
- доступные подписи полей;
- безопасность внешних ссылок;
- lazy-loading и inline-стили;
- размеры CSS, JavaScript и изображений.

Нулевой baseline является блокирующим: новая ошибка или превышение бюджета останавливает CI.

Основные PR: #177, #179, #180, #181, #182, #183 и #184.

### Visual regression и структура CSS

В основной ветке есть:

- страница `/css-maintenance/`;
- `data/css_maintenance_inventory.csv`;
- ссылка на CSS-карту из `/site-health/`;
- базовый аудит CSS-инвентаризации;
- `data/css_regression_matrix.csv` с 14 контрольными сценариями;
- 14 утверждённых PNG и `docs/visual-baseline/manifest.json` с SHA-256;
- read-only Playwright capture workflow и pixel comparator;
- 14 логических разделов `assets/css/styles.css` без изменения каскада;
- канонический CSS fingerprint и блокирующий аудит структуры.

Все 14 сценариев основной ветки имеют статус `passed` и существующий `evidence_ref`. Аудит проверяет manifest, размеры и хеши evidence, браузерную диагностику, допустимый антиалиасинг и отсутствие значимых визуальных изменений.

Issue #165 закрыта как выполненная. Visual baseline перенесён и проверен отдельными PR #225, #228, #231 и #232; сопровождаемая структура CSS слита в PR #255. Любое новое содержательное изменение CSS по-прежнему требует обновления fingerprint и visual comparison.

### Удаление постороннего `nav-v2`

Подтверждено, что `nav-v2` относится к отдельному приложению «Навигатор сделок», а не к порталу ТОС БГО. Канонический репозиторий — `deputat36/vktg`.

После проверки 15 июля 2026 года:

- два файла подтверждены одинаковыми blob SHA в обоих репозиториях;
- локальный `role-menu-v2.js` признан устаревшей копией более новой реализации из `vktg`;
- целевые страницы и Supabase-контур подтверждены в каноническом репозитории;
- три чужих файла удалены из портала ТОС;
- `data/foreign_module_inventory.json` хранит canonical paths, blob SHA и отношения версий;
- CI запрещает возвращение удалённых путей, HTML-ссылок и чужой Supabase-конфигурации.

История файлов сохранена в Git. Публичный код и данные ТОС не изменялись. Связано: issue #280.

### Маршрутная архитектура

Проведена инвентаризация восьми групп и 39 служебных маршрутов.

Закреплены роли:

1. `/workbench/` — главный вход редактора.
2. `/update-tos/` — публичная передача сведений и материалов.
3. `/verification-guide/` — основная методика проверки.
4. `/site-health/` — единственная центральная управленческая сводка.
5. Проектные инструкции отделены от заполняемых и печатных форм.

Автоматических кандидатов на удаление или архив сейчас нет.

Основные PR: #186, #189, #190, #191, #192, #193 и #194.

## Защита четырёх приоритетных карточек

Приоритетные карточки:

1. ТОС «Ивановка».
2. ТОС «Подстёпки».
3. ТОС «Губари».
4. ТОС «Танцырей».

### Evidence-guard

Слит PR #217.

CI сравнивает защищённые поля карточек с базовой веткой. Изменение председателя, границ, телефонов, email, социальных ссылок, логотипа или trust-метаданных требует:

- даты ответа;
- типа источника;
- публичной ссылки либо отметки о сохранённом закрытом источнике;
- решения редактора `partial` или `verified`;
- подтверждения изменяемой группы сведений;
- согласия или открытого основания для публикации контактов и материалов.

Self-test доказывает три сценария:

- неизменённая карточка проходит;
- добавление телефона без доказательств блокируется;
- подтверждённое изменение с источником и согласием проходит.

### Readiness-отчёт

Слиты PR #218, #219, #221 и #222.

Создан `data/priority_tos_update_readiness.json`. Он объединяет трекер отправки и реестр разбора ответов, но не содержит контакты, закрытую переписку, текст ответов и приватные источники.

Текущий статус:

- Ивановка — `find_channel`, нужно найти рабочий канал связи;
- Подстёпки — `find_channel`, нужно найти рабочий канал связи;
- Губари — `ready_to_send`, запрос подготовлен, но не отправлен;
- Танцырей — `ready_to_send`, запрос подготовлен, но не отправлен;
- готовы к обновлению карточки: 0 из 4;
- ожидают внешнего действия: 4 из 4.

Readiness встроен в центральный `data/site_health.json`. `/site-health/` использует один отчёт и сразу показывает стадию, блокировки и следующий шаг. Отдельный второй fetch и `MutationObserver` удалены.

Статический readiness-якорь существует в HTML до выполнения JavaScript, поэтому внутренние ссылки корректно проверяются автоматическим аудитом.

## Готовность к работе с персональными данными

Портал работает в режиме `pre_legal_readiness`. Реестр `data/personal_data_readiness.json` фиксирует восемь обязательных решений; все они остаются `pending` до назначения оператора и профильной юридической проверки.

До завершения P0:

- сбор персональных данных и подписанных согласий на сайте выключен;
- автоматическая публикация из обращений выключена;
- `/privacy/` является редакционной памяткой, а не утверждённой политикой;
- оригиналы согласий и закрытые доказательства запрещено хранить в публичном репозитории;
- временный маршрут исправления и удаления не считается окончательной юридической процедурой.

Блокирующий аудит проверяет эти ограничения в `audit:all`, project-mode и основном workflow.

Обезличенная инвентаризация текущих 24 карточек показывает:

- персональные сведения присутствуют во всех 24 карточках;
- телефон опубликован в 20 карточках, email — в 8;
- личный профиль обнаружен в 11 карточках;
- `source_ref` и `publication_consent_ref` отсутствуют во всех 24 карточках;
- все 24 карточки требуют проверки основания публикации.

`data/publication_basis_inventory.json` хранит только slug, типы полей, количества и флаги готовности. Значения ФИО, контактов и URL в отчёт не копируются; его статусы являются редакционным сигналом, а не юридическим заключением.

На основе инвентаризации сформирована обезличенная очередь из 24 задач:

- волна 1 — 13 карточек с личным профилем, сочетанием телефон+email или нераспознанной ссылкой;
- волна 2 — 9 карточек с телефоном или email;
- волна 3 — Ивановка и Подстёпки, где требуется подтвердить актуальность и основание публикации имени председателя.

Все строки имеют статус `pending_external_confirmation`. Генерация очереди не означает отправку запроса и не изменяет карточки ТОС.

Для трёх волн подготовлены редакционные шаблоны фактического подтверждения. Они не содержат адресатов, контактов или готовых URL, не являются формами согласия и не создают `publication_consent_ref`. Публично разрешена только обезличенная структурированная фиксация результата; свободный текст ответа и сырая переписка остаются вне репозитория. Статусы отправки и получения меняются только после реального внешнего действия.

Шаблоны и их ограничения проверяются в `audit:all`, обоих project-mode и отдельном read-only workflow. Сообщения по этим шаблонам ещё не отправлялись.

## Сохранение старых URL проектов

Для 14 устаревших URL банка проектов введены локальные `noindex,follow`-редиректы на действующие страницы или каталог проектов.

Генератор:

- удаляет только страницы с точным marker автогенерации из `data/projects.json`;
- не удаляет ручные страницы без marker;
- пересоздаёт известные legacy URL по централизованной карте;
- исключает legacy URL из sitemap.

После генерации основной workflow запускает strict-проверку наличия редиректов, их целей, canonical, marker и отсутствия в sitemap. Отдельный временный workflow с правом записи не используется.

## Что реализовано

### Публичная часть

- главная страница и каталог ТОС;
- отдельные карточки территорий;
- новости, проекты, потребности, события и результаты;
- страницы для жителей, председателей, партнёров и инициативных групп;
- карта, поиск, FAQ, контакты и правовой навигатор;
- практические маршруты действий;
- страницы населённых пунктов;
- sitemap, RSS, Open Graph, canonical и структурированные данные.

### Документы и проектная работа

- комплекты для собраний и создания ТОС;
- паспорт проекта и отдельная заполняемая печатная форма;
- карточка проблемы, смета и календарный план;
- письмо партнёру и чек-лист готовности;
- фотоотчёт и итоговый отчёт проекта;
- набор подготовки конкурсной заявки;
- библиотека проектных форм;
- маршруты от потребности до результата и архива.

### Редакционная работа

- `/workbench/` — главный вход в рабочую панель;
- `/workbench-routes/` — короткие сценарии;
- `/collection-board/` — локальные статусы сбора;
- `/outreach-register/` — журнал исходящих запросов;
- `/reply-review/` — разбор полученных ответов;
- `/verification-guide/` — методика проверки;
- `/verification-tasks/` и `/verification-control/` — инструменты редактора;
- `/data-requests/priority-tos/` — рабочая сводка четырёх карточек;
- `/site-health/` — центральная управленческая сводка;
- `/audit/`, `/data-quality/`, `/open-data/`, `/github-tasks/`, `/actions-check/` — специализированные инструменты.

### Автоматические проверки основной ветки

- синтаксис JavaScript;
- JSON и CSV;
- внутренние ссылки и статические якоря;
- title, description, canonical, Open Graph и h1;
- дубли title и description;
- sitemap, RSS и page index;
- даты, идентификаторы, пути и статусы;
- целостность карточек, проектов, потребностей, результатов и новостей;
- происхождение контента;
- доступность и бюджеты ресурсов;
- CSS-инвентаризация и структура матрицы контрольных страниц;
- карантин посторонних модулей;
- роли служебных маршрутов;
- реестры источников, запросов, подтверждений и решений;
- evidence-guard приоритетных карточек;
- readiness и его соответствие центральному `site_health.json`.

Pixel-based visual regression является read-only проверкой основной ветки: 14 сценариев защищены утверждёнными evidence-файлами и comparator.

## Закрытый PR #220 и декомпозиция

PR #220 закрыт без слияния после расхождения с `release-2025-12-22`. Он не является кандидатом на повторное открытие, прямое слияние или массовый cherry-pick.

`data/pr220_decomposition_inventory.json` классифицирует 99 путей закрытого PR. Уже перенесены отдельными пакетами и защищены текущим CI:

- безопасная очистка generated pages;
- user-journey matrix и аудит;
- локальные admin surface/productivity проверки;
- visual capture, утверждённый baseline и comparator;
- структурирование CSS и fingerprint-gate.

Оставшиеся элементы инвентаризации могут рассматриваться только по одному от актуальной основной ветки. Старые результаты PR #220 не считаются доказательством для текущего main.

Безопасная стратегия:

1. Не сливать PR #220 целиком.
2. Разделить его на небольшие тематические пакеты.
3. Создавать новые ветки от актуального `release-2025-12-22`.
4. Переносить только отсутствующие изменения.
5. Не перезаписывать readiness, evidence-guard, центральный `site_health.json` и актуальную документацию.
6. Повторно запускать CI и visual capture на каждом пакете.

## Главные оставшиеся проблемы

### 1. Получение реальных подтверждений

Технический контур готов, но реальные ответы пока не получены.

Нужно:

- найти канал связи для Ивановки и Подстёпок;
- после отдельного разрешения вручную отправить запросы по Губарям и Танцырею;
- фиксировать канал, дату, ответ, источник и согласие;
- обновлять только подтверждённые поля;
- не повышать статус до `verified` автоматически.

### 2. Недостаток подтверждённого контента

Следующий рост сайта зависит от получения:

- подтверждённых новостей;
- реальных проектов и их статусов;
- фотографий до и после;
- актуальных потребностей;
- источников, дат и разрешений на публикацию.

Техническая заполненность не должна подменять реальные события и результаты.

### 3. Официальный реестр ТОС

Импорт и сверка расширенного перечня заблокированы до получения официального актуального реестра ТОС БГО.

### 4. Ручная проверка Pages

Issue #164 остаётся открытой: нужно вручную проверить настройки GitHub Pages и deployment в интерфейсе GitHub.

## GitHub-задачи

Активные:

- #9 — общий аудит и системное улучшение проекта;
- #34 — подтверждение четырёх карточек высокого приоритета;
- #164 — ручная проверка GitHub Pages deployment;
- #166 — отправка подготовленных запросов только после реального действия;
- #205 — правовые и персональные P0-риски.

Завершённые технические задачи:

- #165 — visual baseline и безопасная структура CSS внедрены;
- PR #220 — закрыт без слияния, необходимые части декомпозированы.

## Следующие автономные шаги

1. Поддерживать нулевой SEO/link/accessibility/performance baseline.
2. Поддерживать согласованность readiness, evidence-реестров и `site_health.json`.
3. Рассматривать оставшиеся элементы закрытого PR #220 только как независимые кандидаты от актуального main.
4. Улучшать существующие страницы без создания дублирующих маршрутов.
5. Не создавать шаблонные публикации ради формальной заполненности.
6. Подготавливать инструменты обработки ответов, не отмечая запросы отправленными.
7. После получения официального реестра выполнить отдельную сверку состава ТОС.

## Внешние действия и блокировки

- отправка исходящих запросов требует отдельного подтверждения пользователя;
- карточки нельзя переводить в `verified` без источника, даты и разрешений;
- новые контакты нельзя публиковать без открытого источника или согласия;
- импорт расширенного перечня заблокирован до официального реестра;
- реальные фото и логотипы нельзя использовать без понятного права на публикацию;
- содержательные CSS-изменения требуют нового fingerprint и успешного visual comparison;
- решение по `nav-v2` требует подтверждения владельца проекта;
- закрытый PR #220 нельзя использовать как прямой merge- или массовый cherry-pick-кандидат.
