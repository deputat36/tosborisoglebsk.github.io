# Состояние проекта

Обновлено: 20 июня 2026 года.

## Реализовано

- публичный каталог и отдельные карточки ТОС;
- новости, проекты, потребности, события и истории результатов;
- разделы для жителей, партнёров и председателей;
- комплекты документов для собраний, проектов и создания ТОС;
- правовой навигатор;
- рабочий аудит заполненности и проверки карточек;
- автоматическая генерация страниц и sitemap;
- проверки JSON, SEO, JS-синтаксиса и внутренних ссылок;
- интерактивный центр передачи материалов в разделе `/update-tos/`;
- рабочий аудит сайта и план улучшений в `docs/AUDIT-2026-06-18.md`;
- подготовлен публичный блок статуса сведений для карточек ТОС;
- улучшена страница `/data-quality/` с пояснением статусов проверки;
- добавлена памятка `/chairperson/verify-card/` для подтверждения карточки ТОС;
- улучшен поиск по инструкциям председателя;
- улучшено мобильное меню сайта;
- добавлена runtime-защита legacy-ссылок `/update-tos/` до полной автогенерации страниц;
- расширена внутренняя памятка редактора по ручному запуску GitHub Actions;
- подготовлен публичный коммуникационный набор для запуска портала;
- улучшена рабочая страница `/data-requests/` для первоочередного сбора подтверждений;
- создана GitHub-задача #34 для подтверждения четырёх карточек высокого приоритета;
- в задачу #34 добавлены готовые короткие сообщения для отправки по каждому из четырёх ТОСов;
- в `/communication-kit/` добавлен публичный пост для сбора сведений по Ивановке, Подстёпкам, Губарям и Танцырею;
- повторно обновлён `data/regeneration_request.json` для запроса пересборки сгенерированных карточек;
- создана GitHub-задача #41 для ручного запуска `Generate TOS pages` и проверки HTML-карточек;
- добавлен свежий аудит `docs/AUDIT-2026-06-19.md` с автономным планом улучшений;
- улучшена страница `/site-health/`: fallback без `data/site_health.json`, безопасный вывод данных и `noindex,nofollow`;
- рабочие страницы `/collection-board/` и `/verification-tasks/` закрыты от индексации через `noindex,nofollow`;
- генератор `sitemap.xml` теперь пропускает страницы с `noindex`;
- страница `/verification-tasks/` безопаснее выводит данные аудита и ведёт сразу в сценарий уточнения конкретного ТОС;
- публичные, методические и рабочие входы в `/update-tos/` переведены на сценарный формат `?type=<scenario>#message-builder`;
- раздел `/calendar/` переведён на сценарий `type=event` для добавления событий;
- добавлен отсутствующий `assets/js/events.js`, который выводит события из `data/events.json` и заполняет фильтры календаря;
- старые страницы `/news/view.html`, `/tos/view.html` и локальный инструмент `/tools/import.html` закрыты от индексации через `noindex,nofollow`;
- `assets/js/site.js` исправляет оставшиеся legacy-ссылки на лету, включая старые HTML-карточки и страницы территорий `places/*`.

## Текущий этап

Переход от технической подготовки к запуску, сбору подтверждённых сведений и повышению технической устойчивости рабочих страниц.

## Последнее улучшение

Проведён аудит текущей структуры портала. Проверен пользовательский путь через конструктор `/update-tos/`, добавлена совместимость старых сценарных ссылок, обновлены входы на конструктор с главной страницы и страницы новостей, а технический отчёт состояния сайта усилен проверкой внутренних якорей.

Доработан генератор карточек ТОС: добавлен публичный статус сведений, а старые ссылки из карточек заменены на сценарные ссылки конструктора `/update-tos/?tos=<slug>&type=<scenario>#message-builder`.

Раздел `/data-quality/` теперь объясняет разницу между заполненностью и подтверждением сведений. Для председателей добавлена отдельная памятка `/chairperson/verify-card/` с чек-листом и готовым текстом для отправки уточнений.

Старые открытые PR разобраны: полезные части перенесены точечно, устаревшие ветки закрыты. В поиск добавлены отдельные инструкции председателя, а мобильное меню теперь закрывается по Escape, клику вне меню и переходу по ссылке.

Добавлена общая runtime-защита legacy-ссылок `/update-tos/`: пока старые HTML-страницы не пересобраны генераторами, `site.js` исправляет ссылки вида `/update-tos/`, `/update-tos/#template-*` и `/update-tos/?tos=<slug>` на сценарные URL с `type` и `#message-builder`. Для карточек ТОС дополнительно добавляется блок статуса сведений из `/data/tos_content_audit.json`.

Внутренняя памятка редактора `_private/editor-guide.md` дополнена ручным запуском workflow `Generate TOS pages`, признаками успешной генерации и чек-листом проверки карточек.

Страница `/communication-kit/` расширена до публичного пакета запуска: закреплённый пост, короткий анонс, сообщение председателю, персональный шаблон с `SLUG-ТОС` и ссылки на сценарии `/update-tos/`.

Страница `/data-requests/` теперь содержит прямые сценарные ссылки в сообщениях и отдельный блок «Первые 4 карточки для обработки»: Ивановка, Подстёпки, Губари, Танцырей.

Создана рабочая задача GitHub #34 «Получить подтверждение сведений по 4 карточкам высокого приоритета» с чек-листом, ссылками на карточки и ссылками на формы уточнения. В задачу добавлен комментарий с короткими готовыми сообщениями для отправки по Ивановке, Подстёпкам, Губарям и Танцырею.

В `/communication-kit/` добавлен отдельный публичный пост для ВК «Нужны сведения по 4 ТОСам», чтобы быстро собрать данные по Ивановке, Подстёпкам, Губарям и Танцырею.

Проверка показала, что HTML-карточка `/tos/ivanovka/` всё ещё содержит старые ссылки `/update-tos/?tos=ivanovka` и `/update-tos/#template-*`, а страницы `places/*` могут содержать старую кнопку `/update-tos/`. Поэтому `data/regeneration_request.json` повторно обновлён 19 июня 2026 года с явным списком карточек для проверки после пересборки, а `site.js` временно исправляет legacy-ссылки на стороне браузера.

Новый автокоммит `Generate SEO pages, sitemap and audit` после повторного запроса не найден через доступную проверку. Поэтому создана задача #41 для ручного запуска workflow `Generate TOS pages` и проверки карточек Ивановка, Подстёпки, Губари, Танцырей.

Добавлен свежий аудит `docs/AUDIT-2026-06-19.md`. Первые автономные улучшения уже выполнены: `/site-health/` показывает fallback-сводку по аудиту ТОС, если полного отчёта нет; рабочие страницы `/site-health/`, `/collection-board/`, `/verification-tasks/` закрыты от индексации.

Дополнительно усилены технические проверки: `scripts/generate_sitemap.js` больше не добавляет в `sitemap.xml` страницы с `noindex`, а `assets/js/verification-tasks.js` экранирует данные из JSON и ведёт редактора сразу в сценарий уточнения выбранного ТОС.

После проверки подключений найден отсутствующий файл `/assets/js/events.js`: страница `/calendar/` подключала его, но список событий не мог отрисоваться. Файл добавлен: календарь теперь загружает `data/events.json`, заполняет фильтры и выводит карточки событий. Старые служебные страницы `/news/view.html`, `/tos/view.html` и `/tools/import.html` помечены `noindex,nofollow`, чтобы не конкурировать с актуальными публичными страницами и сгенерированными карточками.

## Следующие задачи

1. Выполнить задачу #41: вручную запустить `Generate TOS pages` на ветке `release-2025-12-22`.
2. Проверить, появился ли новый автокоммит `Generate SEO pages, sitemap and audit` после ручного запуска.
3. Проверить, что сгенерированные карточки ТОС и страницы `places/*` уже содержат сценарные ссылки без необходимости runtime-перехвата.
4. Проверить `/calendar/`: список событий должен отрисовываться из `data/events.json`, а фильтры должны работать по типу события и ТОС.
5. Проверить `/site-health/`: при отсутствии `data/site_health.json` должна отображаться fallback-сводка по аудиту ТОС.
6. Проверить, что после генерации `sitemap.xml` не содержит страниц с `noindex`.
7. Проверить опубликованные карточки ТОС: статус сведений, кнопки уточнения, новости, проекта, потребности и фотоотчёта.
8. Использовать `/communication-kit/` для публикации закреплённого поста и обращения к председателям.
9. Использовать `/data-requests/`, GitHub-задачу #34 и отдельный пост в `/communication-kit/` для первоочередной отправки запросов по Ивановке, Подстёпкам, Губарям и Танцырею.
10. Перевести первые подтверждённые карточки в статус `verification_status: verified` с датой и источником.
11. Добавить проверенные новости, проекты, потребности и результаты по каждому ТОС.
12. Регулярно отслеживать устаревшие данные через `/audit/` и отчёт состояния сайта.
