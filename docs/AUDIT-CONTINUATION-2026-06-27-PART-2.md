# Продолжение аудита портала ТОС БГО от 27 июня 2026 года, часть 2

Этот журнал продолжает `docs/AUDIT-CONTINUATION-2026-06-27.md` после пункта 75.

## 76. Машинная проверка страницы конкурсов и грантов

- добавлен клиентский рендер `assets/js/grants.js` для витрины возможностей из `data/grants.json`;
- `grants/index.html` теперь подключает `assets/js/grants.js`, поэтому контейнер `#grants-list` получает карточки возможностей без ручной правки HTML;
- добавлен аудит `grants/index.html`, `assets/js/grants.js` и `data/grants.json`;
- индекс грантов проверяется на язык страницы, title, canonical, Open Graph URL, h1, контейнер списка, региональную экосистему, конкурс проектов ТОС, ассоциации, ссылки на проекты, документы и отправку проекта;
- закрепляется редакционный принцип: федеральные платформы остаются дополнительным уровнем, а сроки и условия нужно сверять по официальным каналам;
- JS-проверка требует загрузку `data/grants.json`, скрытие черновиков, экранирование HTML, безопасные внешние ссылки, вывод подготовительных шагов и проектных ссылок;
- `data/grants.json` проверяется на обязательные региональные возможности: конкурс проектов ТОС, ассоциацию муниципальных образований, объединения ТОС, «Образ будущего» и «Родные берега»;
- каждая возможность должна иметь id, title, category, status, amount, deadline, difficulty, best_for, note, подготовительные шаги и корректный список `project_links`;
- проверка подключена к `scripts/audit_project_mode.js` и `scripts/audit_project_mode_full.js` рядом с проектным контуром.

## 77. Машинная проверка страницы материалов

- добавлен `data/materials.json` как навигационный список существующих справочных материалов, инструкций и рабочих маршрутов портала;
- добавлен клиентский рендер `assets/js/materials.js` для контейнера `#articles-list` на странице `/materials/`;
- `materials/index.html` теперь содержит загрузочный резерв и подключает `assets/js/materials.js` после общего скрипта сайта;
- добавлен аудит `materials/index.html`, `assets/js/materials.js` и `data/materials.json`;
- проверяются язык страницы, title, canonical, Open Graph URL, h1, ключевая справка `tos-bgo-in-numbers`, контейнер списка, загрузочный текст и подключение JS;
- JS-проверка требует загрузку `data/materials.json`, скрытие черновиков, экранирование HTML, безопасные внешние ссылки, вывод аудитории и тегов;
- каждая запись материала должна иметь id, title, category, audience, description, url, status и теги;
- локальные ссылки из `data/materials.json` проверяются через `repoPathExists`, чтобы в индекс материалов не попадали битые маршруты;
- проверка подключена к `scripts/audit_project_mode.js` и `scripts/audit_project_mode_full.js` рядом с другими публичными разделами.

## 78. Машинная проверка страницы документов

- добавлен клиентский рендер `assets/js/documents.js` для контейнера `#documents-list` на странице `/documents/`;
- `documents/index.html` теперь подключает `assets/js/documents.js` после общего скрипта сайта;
- добавлен аудит `documents/index.html`, `assets/js/documents.js` и `data/documents.json`;
- проверяются язык страницы, title, canonical, Open Graph URL, h1, блок готовых комплектов, контейнер списка, загрузочный текст и подключение JS;
- JS-проверка требует загрузку `data/documents.json`, экранирование HTML, нормализацию относительных ссылок, безопасные внешние ссылки, вывод статуса, назначения и предупреждения;
- каждая запись документа должна иметь title, type, status, description, use_for, attention, url и date;
- локальные ссылки из `data/documents.json` проверяются через `repoPathExists`, чтобы в библиотеке не появлялись битые файлы или маршруты;
- проверка подключена к `scripts/audit_project_mode.js` и `scripts/audit_project_mode_full.js` рядом с другими публичными разделами.

## Практический эффект

Раздел конкурсов и грантов теперь не остаётся статичной страницей с пустым контейнером: список возможностей реально собирается из `data/grants.json`. Машинная проверка закрепляет региональный приоритет раздела, осторожные формулировки по срокам и условиям, безопасные внешние ссылки и базовую полноту карточек поддержки.

Раздел материалов теперь не остаётся пустой витриной: на `/materials/` выводится проверяемый список уже существующих справочных страниц, документов, маршрутов и чек-листов. Индекс можно расширять через `data/materials.json`, а аудит сразу покажет неполные карточки или битые внутренние ссылки.

Раздел документов теперь выводит библиотеку из `data/documents.json`, а не оставляет на странице только индикатор загрузки. Машинная проверка закрепляет обязательные поля документа, безопасный вывод, нормализацию ссылок и наличие ключевых правовых и рабочих шаблонов.

## Ограничения

- публичные факты, сроки, суммы и условия конкурсов не менялись;
- добавлен только рендер существующих данных и аудит структуры;
- `data/materials.json` собран как навигационный индекс по уже существующим страницам портала, без добавления новых неподтверждённых фактов;
- `data/documents.json` не изменялся, добавлены только рендер и аудит существующей библиотеки;
- актуальность конкретных конкурсов, ссылок, дедлайнов, положений и документов по-прежнему нужно сверять по официальным каналам;
- CI/Actions логи не проверялись.