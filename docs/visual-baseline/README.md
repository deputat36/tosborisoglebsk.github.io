# Доказательства визуальной регрессии

Каталог содержит разрешённые к публикации снимки контрольных страниц портала ТОС БГО.

Основная матрица: `data/css_regression_matrix.csv`.
Правила сопровождения: `docs/CSS-MAINTENANCE.md`.
Структурные пользовательские сценарии: `data/user_journey_matrix.csv`.
Манифест захвата: `docs/visual-baseline/manifest.json`.

## Текущее состояние

13 июля 2026 года зафиксированы все 14 обязательных baseline-кейсов:

- 14 из 14 снимков сохранены в `docs/visual-baseline/`;
- runtime failures: 0;
- quality failures: 0;
- console errors: 0;
- page errors: 0;
- failed requests: 0;
- horizontal overflow: 0;
- светлая и тёмная темы совпадают с матрицей;
- мобильное меню открыто с `aria-expanded=true`;
- print media зафиксирован отдельным кейсом.

Источник: GitHub Actions capture run `29262442927`. SHA-256 каждого PNG записан в `manifest.json` и проверяется скриптом `scripts/audit_css_regression_matrix.js`.

## Что считается доказательством

Поле `evidence_ref` может содержать:

- путь к существующему файлу в публичном репозитории, например `docs/visual-baseline/css-reg-001.png`;
- ссылку `https://...` на вложение GitHub issue или pull request;
- ссылку на иной устойчивый источник, доступный проверяющему без закрытых учётных данных.

Локальный путь обязан существовать. Произвольный текст, имя файла без пути и ссылка на закрытый личный диск доказательством не считаются.

## Имена файлов

Текущий baseline использует строгую связь имени с идентификатором матрицы:

`<case_id>.png`

Примеры:

- `css-reg-001.png` — главная, desktop, светлая тема;
- `css-reg-003.png` — главная, mobile, открытое меню;
- `css-reg-007.png` — карточка ТОС, mobile, тёмная тема;
- `css-reg-014.png` — печатная версия чек-листа.

Не включайте в имя ФИО, телефон, адрес, email или другие персональные данные.

## Минимальный состав доказательства

На снимке должны быть видны:

- проверяемая область страницы;
- состояние темы;
- открытое меню или другое действие, если оно указано в матрице;
- отсутствие горизонтальной прокрутки, наложений и обрезания ключевых элементов;
- для печати — результат print media, а не только экранная версия.

## Безопасность

Перед публикацией снимка проверьте:

- нет ли закрытых персональных данных;
- нет ли черновых обращений, переписки и служебных токенов;
- разрешено ли публиковать показанные фотографии и логотипы;
- не отображаются ли данные из локального хранилища браузера;
- не попали ли в кадр вкладки, уведомления или данные других систем.

При сомнении используйте отдельный чистый профиль браузера и публичную версию сайта.

## Статусы матрицы

- `baseline_required` — доказательства ещё нет; `evidence_ref` должен быть пустым;
- `baseline_captured` — исходный снимок сохранён, файл существует и его SHA-256 совпадает с manifest;
- `passed` — после изменения выполнено сравнение с baseline;
- `failed` — найдено отличие, в `notes` описана проблема;
- `blocked` — проверка невозможна, в `notes` указана конкретная причина.

Статус `passed` нельзя использовать как замену `baseline_captured`: он означает повторную проверку после изменения.

## Порядок следующего сравнения

1. Внести одно небольшое CSS-изменение.
2. Запустить workflow `.github/workflows/visual-baseline.yml`.
3. Сравнить новый artifact с PNG из `docs/visual-baseline/`.
4. Проверить manifest, overflow, темы, меню и ошибки браузера.
5. При отсутствии отличий установить `passed` для затронутых случаев.
6. При отличиях установить `failed` и описать наблюдаемую регрессию.
7. Запустить `npm run audit:css-regression` и полный project-mode audit.
