# Сравнение visual baseline после структурирования CSS

Дата проверки: 13 июля 2026 года.

## Проверяемое изменение

В `assets/css/styles.css` добавлены только комментарии-разделители между существующими логическими группами. Селекторы, значения свойств и порядок CSS-правил не изменялись.

## Источники сравнения

- утверждённый baseline: GitHub Actions run `29262442927`, visual run №14;
- проверяемый capture: GitHub Actions run `29262907242`, visual run №21;
- commit проверяемого изменения: `19422009105a930db50902a2079ce0a7c6865140`;
- матрица: `data/css_regression_matrix.csv`;
- утверждённые снимки: `docs/visual-baseline/css-reg-001.png` — `css-reg-014.png`.

## Результат

- контрольных случаев: 14;
- совпали по размеру: 14 из 14;
- пиксельно идентичны: 14 из 14;
- побайтово идентичны: 13 из 14;
- изменённых пикселей: 0;
- runtime failures: 0;
- quality failures: 0;
- horizontal overflow: 0;
- console errors: 0;
- page errors: 0;
- failed requests: 0.

Случай `css-reg-007` имеет другой SHA-256 PNG-файла, но декодированные RGBA-пиксели полностью идентичны. Отличие относится только к бинарному представлению PNG и не является визуальной регрессией.

## Вывод

Комментарии-разделители не изменили внешний вид ни одного из 14 контрольных состояний. Все случаи могут быть переведены из `baseline_captured` в `passed` для данного этапа CSS-сопровождения.

Дальнейшее изменение CSS требует нового запуска `.github/workflows/visual-baseline.yml` и сравнения с утверждёнными файлами в `docs/visual-baseline/`.
